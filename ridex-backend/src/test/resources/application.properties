# Tests commit real rows, so they get their own database - never the one the apps run against.
# Overridable for CI or a different host with RIDEX_TEST_DB_URL.
spring.datasource.url=${RIDEX_TEST_DB_URL:jdbc:postgresql://localhost:5432/ridex_platform_test}
